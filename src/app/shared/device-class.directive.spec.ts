import {DeviceClassDirective} from './device-class.directive';

describe('DeviceClassDirective', () => {
  it('should create an instance', () => {
    const directive = new DeviceClassDirective();
    expect(directive).toBeTruthy();
  });
});
