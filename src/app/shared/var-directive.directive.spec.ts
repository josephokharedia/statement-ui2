import { VarDirectiveDirective } from './var-directive.directive';

describe('VarDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new VarDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
